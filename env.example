# Stripe API Keys
# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLIC_KEY=pk_test_your_publishable_key_here

# Stripe Product Configuration
# Create a product in Stripe Dashboard and copy the Price ID
STRIPE_PRICE_ID_EBOOK=price_your_price_id_here

# URLs
# For local development:
STRIPE_SUCCESS_URL=http://localhost:3000/ebook/acesso
STRIPE_CANCEL_URL=http://localhost:3000/checkout/cancelado

# For production, use your actual domain:
# STRIPE_SUCCESS_URL=https://your-domain.com/ebook/acesso
# STRIPE_CANCEL_URL=https://your-domain.com/checkout/cancelado

# Webhook Secret
# For local development, use Stripe CLI:
# Run: stripe listen --forward-to localhost:3000/api/stripe/webhook
# Copy the webhook signing secret (whsec_...)
STRIPE_WEBHOOK_SECRET_EBOOK=whsec_your_webhook_secret_here

# For production, get this from:
# Stripe Dashboard → Developers → Webhooks → Your endpoint → Signing secret

